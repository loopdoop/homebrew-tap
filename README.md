# homebrew-tap

Homebrew tap for the [skl](https://useskl.com) CLI.

```sh
brew install loopdoop/tap/skl
```

The formula (`Formula/skl.rb`) is auto-updated on each release by
`release-cli.yml` in the (private) source repo. Releases live at
<https://github.com/loopdoop/skl/releases>.
